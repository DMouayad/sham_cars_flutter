import 'package:sham_cars/utils/utils.dart';

class LoginFormHelper extends BaseFormHelper {
  // final TextEditingController emailOrPhoneController = TextEditingController();

  // String get emailOrPhoneValue => emailOrPhoneController.text;
  // String? emailOrPhoneValidator(String? value, BuildContext context) {
  //   if (value?.isEmpty ?? true) {
  //     return context.l10n.emailOrPhoneIsRequired;
  //   } else {
  //     if (!isValidEmail(value) && !isValidPhoneNo(value)) {
  //       return context.l10n.emailOrPhoneIsInvalid;
  //     }
  //   }
  //   return null;
  // }
}
